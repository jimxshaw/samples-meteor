// Any JS here is automatically run for us. 

// Import the React library.
import React from 'react';

// Create a component. A component's job is to render HTML to the screen.
const App = () => {
	return (
		<div>
			React App #2
		</div>
	);
};


// Render the component to the screen.

